<section>
        <h1 style="margin: 20px 0 20px;"><%= __('common.tags') %> </h1>
        <% (page._tags || page.tags).forEach(tag => {%>
        <span class="hometag">
            <a style="text-decoration: none;" href="<%- url_for(tag.path) %>"><%= i18n_for_tag(tag.name) %> - <%- tag.real_length %>/<%- tag.length %></a>
        </span>
        <% })%>
</section>